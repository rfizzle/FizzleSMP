name: Companion Tests

on:
  push:
    branches: [master]
    paths:
      - 'companions/meridian/**'
      - '.github/workflows/companion-tests.yml'
  pull_request:
    paths:
      - 'companions/meridian/**'
      - '.github/workflows/companion-tests.yml'
  workflow_dispatch:

jobs:
  meridian:
    name: meridian (test + runGametest)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: companions/meridian
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('companions/meridian/**/*.gradle*', 'companions/meridian/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Run unit tests
        run: ./gradlew test --no-daemon

      - name: Run gametests
        run: ./gradlew runGametest --no-daemon

      - name: Upload junit reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: meridian-junit
          path: |
            companions/meridian/build/test-results/test/*.xml
            companions/meridian/build/junit-gametest.xml
            companions/meridian/build/reports/tests/test/

      - name: Upload gametest crash reports on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: meridian-crash-reports
          path: companions/meridian/build/gametest/crash-reports/
