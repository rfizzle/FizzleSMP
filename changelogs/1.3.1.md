## Fixed
- RpgDifficulty was shipped as server-only in v1.3.0, but the mod modifies entity attributes and tracked data that the client must know about. Missing it caused a crash when the server sent tracked data for a scaled mob. Now included on both sides.
